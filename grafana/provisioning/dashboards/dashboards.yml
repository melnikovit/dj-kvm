apiVersion: 1
providers:
  - name: dj-kvm
    orgId: 1
    folder: dj-kvm
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
